# Z-Score Calculator

A simple command-line tool to calculate z-scores.

## Installation

Download the pre-built binary from the releases page or build from source:

```bash
go install github.com/darius/zscore-calculator/cmd/zscore@latest
